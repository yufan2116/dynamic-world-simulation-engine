# Legacy demo assets (not loaded at runtime)

- `demo_story.json` — 旧 step/node 视觉小说结构（含 `next_step`），已由 `ravenford_demo.json` 的 outcome-only 方案替代。

运行时 Demo 入口：`POST /game/new-demo` → `data/demo_scripts/ravenford_demo.json`。
